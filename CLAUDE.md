@AGENTS.md

## Serena
Serena MCP está configurado (`.mcp.json`, `.serena/`). Para explorar y editar código prefiere sus herramientas simbólicas (`get_symbols_overview`, `find_symbol`, `replace_symbol_body`) antes que leer archivos completos. Agente disponible: `backend-developer`.
