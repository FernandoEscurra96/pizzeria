import type { NextConfig } from "next";

// Configuración global de Next.js. Este proyecto no necesita opciones
// especiales (no usamos imágenes remotas, redirects, headers custom, etc.),
// así que queda vacío; Next usa sus valores por defecto.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
